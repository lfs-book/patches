Submitted By: Greg Schafer <gschafer at zip dot com au>
Date: 2003-09-22
Initial Package Version: 4.0.3
Origin: I based this on the original patch. Instead of patching configure.in, patch configure to
        avoid dangerous autotools re-running.
Description: Allow configure script to properly detect libpam.

diff -Naur shadow-4.0.3.orig/configure shadow-4.0.3/configure
--- shadow-4.0.3.orig/configure	2002-03-13 19:04:25.000000000 +0000
+++ shadow-4.0.3/configure	2003-09-22 15:00:42.000000000 +0000
@@ -10262,6 +10262,8 @@
 EOF
 
 		LIBPAM="-lpam"
+                 SAVE_LIBS=$LIBS
+               LIBS="$LIBS $LIBPAM"
 		echo "$as_me:10265: checking for main in -lpam_misc" >&5
 echo $ECHO_N "checking for main in -lpam_misc... $ECHO_C" >&6
 if test "${ac_cv_lib_pam_misc_main+set}" = set; then
@@ -10313,6 +10315,7 @@
 
 fi
 
+               LIBS=$SAVE_LIBS
 		echo "$as_me:10316: checking use login access checking if PAM not used" >&5
 echo $ECHO_N "checking use login access checking if PAM not used... $ECHO_C" >&6
 		cat >>confdefs.h <<\EOF
